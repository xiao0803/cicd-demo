apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cicd-demo

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: cicd-demo
